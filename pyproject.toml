[project]
name = "python-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Alma Zhantleuova", email = "zhantleuova.kz@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "ecbdata>=0.1.1",
    "ipykernel>=7.3.0",
    "matplotlib>=3.11.1",
    "pandas>=3.0.5",
    "seaborn>=0.13.2",
]

[project.scripts]
python-project = "python_project:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
